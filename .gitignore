# ── Secret keys ───────────────────────────────────────────────────────
# NEVER commit `.env` — it may contain GEMINI_API_KEY for optional ML pipeline features (cluster personas).
.env

# Built dashboard is large and regenerated locally.
creative_copilot.html

# Generated data files (large, reproducible from the CSVs)
app_data.js
ml_results.json

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── OS ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
